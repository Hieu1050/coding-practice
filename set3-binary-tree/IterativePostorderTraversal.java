import java.util.List;

public class IterativePostorderTraversal {
    public List<Integer> postorderTraversal(TreeNode root) {
        // left - right - root
        // base case
        // while curr root is not null
            // stack push (root)
            // stack push (right)
            // stakc push (left)
            
    }
}
